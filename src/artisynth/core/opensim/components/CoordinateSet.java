package artisynth.core.opensim.components;

public class CoordinateSet extends SetBase<Coordinate> {

   @Override
   public CoordinateSet clone () {
      return (CoordinateSet)super.clone ();
   }
}
