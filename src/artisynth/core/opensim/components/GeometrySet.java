package artisynth.core.opensim.components;

public class GeometrySet extends SetBase<DisplayGeometry> {

   @Override
   public GeometrySet clone () {
      return (GeometrySet)super.clone ();
   }
}
