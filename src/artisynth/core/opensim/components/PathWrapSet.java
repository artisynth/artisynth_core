package artisynth.core.opensim.components;

public class PathWrapSet extends SetBase<PathWrap> {
   
   @Override
   public PathWrapSet clone () {
      return (PathWrapSet)super.clone ();
   }
   
}
